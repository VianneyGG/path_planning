# Notebooks Directory

Place exploratory or report notebooks in this folder.

Guidelines:

- Keep notebook outputs minimized before commit.
- Move stable logic to Python modules under `src/`.
- Use relative paths via `pathlib.Path` when loading project files.
