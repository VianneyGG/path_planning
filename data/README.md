# Data Directory

Use this folder for local raw/derived datasets that should not be committed by default.

Guidelines:

- Keep immutable source references here (file names, download links, checksums).
- Store large generated outputs under `artifacts/` or external storage.
- Do not commit sensitive data.
